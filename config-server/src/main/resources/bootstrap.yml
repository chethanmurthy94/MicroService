server:
  port: 8889
  
spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Test
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
            
management:
  security:
    enabled: false
