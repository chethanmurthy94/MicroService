spring:
  application:
     name: customer-authentication-us
  cloud:
    config:
      uri: http://localhost:8889
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
          
server:
  port: 8080
        
management:
  security:
    enabled: false
